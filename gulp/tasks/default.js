var gulp = require('gulp');

// This is the default gulp task
// It is executed when you type "gulp" at the command line 
gulp.task('default', function() {
	console.log('This is the default gulp task');
});